{% extends 'base.html.twig' %}

{% block title %}Edit Dormitory{% endblock %}

{% block body %}
    <h1>Edit Dormitory</h1>

    {{ include('dormitory/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_dormitory_index') }}">back to list</a>

    {{ include('dormitory/_delete_form.html.twig') }}
{% endblock %}
