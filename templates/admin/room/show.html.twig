{% extends 'base.html.twig' %}

{% block title %}Room{% endblock %}

{% block body %}
    <h1>Room</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ room.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ room.title }}</td>
            </tr>
            <tr>
                <th>Dormitoryid</th>
                <td>{{ room.dormitoryid }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ room.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ room.image }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ room.price }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ room.status }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ room.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_room_index') }}">back to list</a>


    {{ include('admin/room/_delete_form.html.twig') }}
{% endblock %}
